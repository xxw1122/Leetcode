class Solution {
public:
    vector<int> findSubstring(string s, vector<string>& words) {
        vector<int> res;
        if (words.empty()) return res;
        int lenw = words.size(), lens = s.size(), len = word[0];
        for (int i = 0; i + len * lenw - 1 < lens; i ++) {
        }
    }
};